version: '3.8'

services:
  site-expense-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: site-expense-tracker
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
